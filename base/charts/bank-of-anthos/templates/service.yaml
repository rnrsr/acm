apiVersion: v1
kind: Service
metadata:
  name: {{ include "bank-of-anthos.fullname" . }}
  labels:
    {{- include "bank-of-anthos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bank-of-anthos.selectorLabels" . | nindent 4 }}
