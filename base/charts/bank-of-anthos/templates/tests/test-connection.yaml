apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bank-of-anthos.fullname" . }}-test-connection"
  labels:
    {{- include "bank-of-anthos.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bank-of-anthos.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
